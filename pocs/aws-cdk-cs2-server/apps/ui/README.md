CS2 Pro Max UI (Next.js App Router + Tailwind + Framer Motion)

Quick start:
1. cd packages/ui
2. pnpm install
3. set NEXT_PUBLIC_API_URL in .env.local
4. pnpm dev

This UI uses:
- Next.js App Router
- TailwindCSS
- Framer Motion for subtle animations
- Canvas particles background
