{
  "name": "infra-cdk",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "pnpm build && cdk deploy --all --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.99.0",
    "constructs": "^10.3.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "aws-cdk": "^2.99.0",
    "typescript": "^5.0.0"
  }
}
